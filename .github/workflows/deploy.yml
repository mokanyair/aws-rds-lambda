name: Deploy Serverless INFRA with Terraform to AWS

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  terraform:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.6.6

      - name: Configure AWS Credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v2
        with:
          role-to-assume: arn:aws:iam::866317130608:role/github-actions-terraform-role
          aws-region: us-east-1

      - name: Package Lambda with dependencies
        run: |
          rm -rf build
          mkdir build
          cp lambda/lambda_function.py build/
          pip install pymysql -t build/
          cd build
          zip -r ../lambda_function.zip .

      - name: Terraform Init
        run: terraform init -upgrade

      - name: Terraform Format Check
        run: terraform fmt -check -recursive

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        run: terraform plan

      - name: Terraform Apply
        if: github.ref == 'refs/heads/main'
        run: terraform apply -auto-approve